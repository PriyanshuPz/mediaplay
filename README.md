```
npm install
npm run dev
```

```
open http://localhost:3000
```

Set `TMDB_API_KEY` in your environment to enable thumbnail suggestions for movies and web series. Music suggestions fall back to lightweight external artwork lookup, and manual upload still works if no candidates are found.
